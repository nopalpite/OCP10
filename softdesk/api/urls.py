from django.urls import path
from rest_framework.urlpatterns import format_suffix_patterns
from api import views

# urlpatterns = [
#     path('projects/', views.ProjectViewSet.as_view()),
#     path('projects/<int:pk>/', views.ProjectViewSet.as_view()),
#     path('projects/<int:pk>/users/', views.ContributorViewSet.as_view()),
#     path('projects/<int:pk>/users/<int:pk>', views.ContributorViewSet.as_view()),
# ]

